import API from '@/api'
import {
} from '@/store/mutation-types'
import { parseVideos } from '@/util/parser'

const state = {
}

const getters = {
}

const mutations = {
}

const actions = {
}

export default {
  state,
  getters,
  mutations,
  actions,
}
